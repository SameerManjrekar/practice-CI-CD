name: My First GitHub Action
on: [push]
jobs:
  build-app:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 14
      - name: Install noe modules
        run: npm install
      - name: Build the project
        run: npm run build
      - name: Deploying the project
        env:
          VERCEL_ORG_ID: Q5gMqszqF4BXPCCCxPkXPxYX
          VERCEL_PROJECT_ID: prj_710RMviPvvJ4E0WctjApjW2KpyOO
        run: npm run deploy
      - name: Setup Finish
        run: echo "Yaay! Janely!!"
